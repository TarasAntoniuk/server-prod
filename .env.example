# PostgreSQL Configuration
POSTGRES_HOST=shared-postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_strong_password_here

# Database Names
POSTGRES_DB_FINANCE=finance
POSTGRES_DB_TIMETRACKING=time_tracking

# Finance Database User
FINANCE_DB_USER=finance_user
FINANCE_DB_PASSWORD=FinanceSecurePass123!

# Time Tracking Database User
TIMETRACKING_DB_USER=timetracking_user
TIMETRACKING_DB_PASSWORD=TimeTrackingSecurePass123!

# Deployment Scenarios:
# 1. Docker (default): POSTGRES_HOST=shared-postgres
# 2. External server: POSTGRES_HOST=10.0.0.5 or db.example.com
# 3. Managed DB (AWS RDS, etc): POSTGRES_HOST=xxx.rds.amazonaws.com

# Port Mapping:
# - Internal (Docker): 5432
# - External (during migration): 127.0.0.1:5433
# - External (after migration): 127.0.0.1:5432

# Notes:
# 1. Copy this file to .env and update the values
# 2. Never commit .env file to git
# 3. Use strong passwords in production
# 4. For external PostgreSQL, update POSTGRES_HOST to IP/domain
